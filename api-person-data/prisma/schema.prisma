datasource db {
  provider = "sqlite"
}

generator client {
  provider = "prisma-client-js"
}

model Person {
  id    Int     @id @default(autoincrement())
  name  String
  age   Int
  email String  @unique
}
