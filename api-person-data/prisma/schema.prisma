datasource db {
  provider = "sqlite"
}

generator client {
  provider = "prisma-client-js"
}

model Person {
  id        String   @id @default(uuid())
  name      String
  email     String   @unique
  createdAt DateTime @default(now())
}
